export function getAllUsers(req, res) {
  res.json({ message: "Welcome to the user route" });
}

export function getAllStudents(req, res) {
  res.json({ message: "Welcome to the user route" });
}

export default function getAllMentors(req, res) {
  res.json({ message: "Welcome to the user route" });
}

export function getOneUser(req, res) {
  res.json({ message: "Welcome to the user route" });
}
